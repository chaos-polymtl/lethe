#include <solvers/postprocessing_velocities.h>
/*
template TrilinosWrappers::MPI::Vector
AverageVelocities<2, TrilinosWrappers::MPI::Vector, DoFHandler>::
  calculate_average_velocities(
  const TrilinosWrappers::MPI::Vector &present_solution,
  const Parameters::SimulationControl &simulation_control,
  const Parameters::PostProcessing &   post_processing,
  const double &                       current_time,
  const DoFHandler &                     locally_owned_dofs,
  const DoFHandler &                     locally_relevant_dofs,
  const MPI_Comm &                     mpi_communicator);

template TrilinosWrappers::MPI::Vector
AverageVelocities<3, TrilinosWrappers::MPI::Vector, IndexSet>::
  calculate_average_velocities(
  const TrilinosWrappers::MPI::Vector &present_solution,
  const Parameters::SimulationControl &simulation_control,
  const Parameters::PostProcessing &   post_processing,
  const double &                       current_time,
  const DoFHandler &                     locally_owned_dofs,
  const DoFHandler &                     locally_relevant_dofs,
  const MPI_Comm &                     mpi_communicator);

template TrilinosWrappers::MPI::BlockVector
AverageVelocities<2, TrilinosWrappers::MPI::BlockVector, IndexSet>::
  calculate_average_velocities(
  const TrilinosWrappers::MPI::BlockVector &present_solution,
  const Parameters::SimulationControl      &simulation_control,
  const Parameters::PostProcessing &        post_processing,
  const double &                            current_time,
  const DoFHandler &                     locally_owned_dofs,
  const DoFHandler &                     locally_relevant_dofs,
  const MPI_Comm &                     mpi_communicator);

template TrilinosWrappers::MPI::BlockVector
AverageVelocities<3, TrilinosWrappers::MPI::BlockVector, IndexSet>::
calculate_average_velocities(
  const TrilinosWrappers::MPI::BlockVector &present_solution,
  const Parameters::SimulationControl      &simulation_control,
  const Parameters::PostProcessing &        post_processing,
  const double &                            current_time,
  const DoFHandler &                     locally_owned_dofs,
  const DoFHandler &                     locally_relevant_dofs,
  const MPI_Comm &                     mpi_communicator); */