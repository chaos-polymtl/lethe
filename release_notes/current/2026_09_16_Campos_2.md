## [Master] - 2026/09/16

### Fixed

- MINOR Clang-tidy CI was showing a warning regarding `PostProcessing`, which was initialized as a `struct` in the `.h` file but instantiated as a `class` in the `.cc` file. This PR fixes this by changing `struct` to `class` in the `.h` file. This follows the same logic as the refactoring of the `Mesh` parameters done in #2118 . [#2128](https://github.com/chaos-polymtl/lethe/pull/2128) 