## [Master] - 2026/08/19

### Added

- MINOR Small fixes in grid generators: (i) change `;` to `:` to split grid arguments in `periodic_hills`, which is the separator used in all other lethe meshes; (ii) rename `fichera_oven_grid` to `grid_fichera_oven` so that it follows the same pattern as the other lethe built-in mesh types. [#2097](https://github.com/chaos-polymtl/lethe/pull/2097)